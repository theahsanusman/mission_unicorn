// If initialzing the variabling while declaring it then wirting type isn't important cuz' ts gets it

// let singer = 'A.R';
// singer = 43; Giving Error Like i mentioned
// console.log(singer);

// Here's the another case
// let rocker; // type any by default

// rocker = 'Joan';
// console.log(rocker.toUpperCase());

// rocker = 19;
// console.log(rocker.toPrecision(1));

// rocker.toUpperCase(); Throwing an Error
// Because in any type we can then move it to other types too by assinging that type value like above & can have access properites related to that type
// But as the toUpperCase is not available in number type it's giving an error

// let rocker: string;
// rocker = 'RF';

// import "./chap2_b";

// const shared = '4';