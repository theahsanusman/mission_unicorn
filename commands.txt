compile ts to js, it will follow strict rule from ts file instead of tsconfig while
tsc filename.ts 

compile all ts files to js files, will use tsconfig purley for instructions 
tsc

Get result of ts file without generating js file
ts-node filename.ts

For Output files, go to tsconfig and search outDir

tsc --watch to compile on auto

when not having ts files in main folder where tsconfig is then go into tsconfig and change rootdir

Turn on Declaration from tsconfig to have the filename.d.ts file with compiled js to know functions parameters & returning types